<h1>Edit Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines For Wikis</p>
    <ul>
      <li>Make sure it's from your soul.</li>
      <li>Make sure it touches my heart.</li>
      <li>Don't yell at someone, casue it will bring you nightmare at 12:00am EST.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= render partial: 'form', locals: { wiki: @wiki } %>
  </div>

  <div>
    <p>Add Collaborators</p>
      <% if current_user.id == @wiki.user_id || current_user.admin? %>
        <%= collection_check_boxes(:wiki, User.all) do |bx| %>
            <%= bx.check_box %>
            <%= bx.label %><br />
        <% end %>
      <% end %>
  </div>

</div>
