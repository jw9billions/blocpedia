<div>
  <%= render partial: 'wiki', locals: {wiki: @wiki} %>
</div>

<div class="row">
  <div class="col-md-8">
    <p><%= markdown(@wiki.body) %> <small> by <%= @wiki.user.email %></small></p>
  </div>
<!--
  <% if user_is_authorized_for_wiki?(@wiki) %>
     <div class="col-md-4">
       <%= link_to "Edit Wiki", edit_wiki_path(@wiki), class: 'btn btn-success' %>
       <%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this wiki?' } %>
     </div>
  <% end %>
-->
  <div class = 'col-md-4'>
    <% if current_user %>
       <%= render partial: 'collaborators/collaborator', locals: { wiki: @wiki } %>
    <% end %>
  </div>
</div>
</br>
<div class="col-md-4">
  <%= link_to "Edit Wiki", edit_wiki_path(@wiki), class: 'btn btn-success' %>
  <%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this wiki?' } %>
</div>
